#!/usr/bin/env ruby
# frozen_string_literal: true

require "sora"
require "optparse"

opt = OptionParser.new

params = { port: 8080 }

opt.on("-p [PORT]", "--port [PORT]") { |v| params[:port] = v.to_i }

opt.parse!(ARGV)

Sora.server(**params)
